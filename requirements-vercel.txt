# Minimal requirements for Vercel deployment
# Excludes heavy ML libraries (PyTorch, transformers, etc.)
# Dashboard will be read-only - no scraping/scoring on Vercel

# Ultra-minimal requirements for Vercel mobile remote control
# Just proxies commands to your desktop - no ML, no scraping
Flask==3.0.0
requests==2.31.0
python-dotenv==1.0.0
